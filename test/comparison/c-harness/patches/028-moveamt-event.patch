diff --git a/src/allmain.c b/src/allmain.c
--- a/src/allmain.c
+++ b/src/allmain.c
@@ -155,6 +155,10 @@
     }

     u.umovement += moveamt;
+    /* Log hero movement allocation for parity comparison (028-moveamt-event).
+     * Do NOT call inv_weight() — it sets gw.wc as a side effect. */
+    event_log("moveamt[wtcap=%d moveamt=%d umovement=%d pos=%d,%d]",
+              wtcap, moveamt, u.umovement, u.ux, u.uy);
     if (u.umovement < 0)
         u.umovement = 0;
 }
